<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>

    <h1>Quora Posts</h1>

    <div class="posts-container">
        <% for (let post of posts) { %>
            <div class="post-card">
                <div class="username">@<%= post.username %></div>
                <div class="content"><%= post.content %></div><br><br>
                <a href="/posts/<%= post.id %>"> view post</a>
                <a  href="http://localhost:8080/posts/<%= post.id %>/edit" class="create-btn" >Edit Post</a>
                <hr>
                <form action="/posts/<%=post.id%>?_method=DELETE" method="post">
                   <button>Delete Posts</button> 
                </form>
            </div>
        <% } %>
        <a  href="http://localhost:8080/posts/new" class="create-btn" onclick="">Create A New Post</a>
  
    </div>
    

</body>
</html>
